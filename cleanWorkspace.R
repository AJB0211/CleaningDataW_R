
suppressMessages(remove(
  data,features,freq,activities,subjects,test,test_subjects,time,train_subjects,x,x_test,x_train,x_trim,
  y,y_test,y_train, freqCols,saveDir,srcURL,timeCols,timeLabels,wantedFeatures,freqLabels
))